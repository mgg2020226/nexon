# Security: Wait 7 days before installing newly published packages
# This protects against malware that gets caught and unpublished within hours
minimum-release-age=10080

# Security: Reject packages whose trust signals (provenance, signatures) weaken
trust-policy=no-downgrade
